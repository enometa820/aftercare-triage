# 조사 — 참고구현 (재사용 자산 카탈로그)

> 우리 기능을 구현할 때 바로 참고·재사용할 **기존 코드·설계**를 Scrapling으로 크롤해 추출한 것.
> 방법 = `refs/crawl.py`(Scrapling 정적 Fetcher, stealthy_headers, 14 타깃 status 200). 원본 덤프 = `refs/raw/`(gitignore, 로컬).
> 조사 2026-07-26. 구현 원칙은 [`BP-기술-베스트프랙티스.md`], 컨셉은 [`기획.md`].

각 자산 = **무엇을 주나 → 어떻게 쓸까(우리 모듈 매핑) → 출처**. `[읽음]` = README/소스 직접 확인, `[BP검증]` = BP 조사로 교차확인.

---

## 1. uqlm — 텍스트측 "기권 게이트"의 기성 엔진 `[읽음]`

**무엇:** LLM 불확실성 정량화 라이브러리(Apache-2.0, PyPI `uqlm`). **black-box 스코어러**(logit 불필요, 아무 LLM) 0~1 confidence 반환. 우리가 손으로 짜려던 self-consistency·semantic entropy가 이미 구현됨.

**핵심 API (README 확인):**
```python
from uqlm import BlackBoxUQ
bbuq = BlackBoxUQ(llm=llm, scorers=["semantic_negentropy"], use_best=True)
results = await bbuq.generate_and_score(prompts=prompts, num_responses=5)  # k회 재샘플 → 일관성
results.to_df()   # 응답별 confidence 0~1
```
- black-box 스코어러: `semantic_negentropy`(discrete semantic entropy, Farquhar Nature 2024)·`noncontradiction`·`entailment`·`exact_match`·`bertscore`·`cosine`.
- `UQEnsemble(llm, scorers=[...]).tune(prompts, ground_truth_answers)` → **라벨 캘리브레이션셋으로 임계값·가중치 튜닝**(우리 캘리브레이션셋 접근과 1:1).
- `LLMPanel(judges=[...])` = LLM-as-judge 패널(근거문 품질 채점용, 보조).
- **멀티모달 UQ·score calibration 예제 노트북 존재**(examples/) — 이미지측 기권에도 참고.

**우리 매핑:** 텍스트 판정의 self-consistency 기권 신호를 uqlm로 구현. **Claude는 LangChain `ChatAnthropic`로 래핑해 `llm`에 주입**(uqlm는 LangChain Chat Model 호환). num_responses=5~10. 직접 구현 대비 수일 절약.

출처: github.com/cvs-health/uqlm · 논문 JMLR v27/25-1557, TMLR(WOFspd4lq5) · `refs/raw/uqlm-readme.md`

---

## 2. MAPIE — 3-way 판정에 컨포멀 + recall 보장 `[읽음]`

**무엇:** 분포무관 불확실성·**리스크 제어** 라이브러리(BSD-3, sklearn-contrib). 분류 예측셋 + **Conformal Risk Control(CRC)·Learn-Then-Test(LTT)** 구현, 이론적 보장. 2026 로드맵에 "LLM-as-Judge 리스크 제어·exchangeability 검정" 추가 예정.

**우리 매핑:** 우리 3-way(정상/불확실/레드플래그) 위에 **split conformal**로 "불확실이면 기권" 임계값을 데이터로 결정. 나아가 **CRC로 레드플래그 False Negative Rate에 기대값 상한 보장**(BP의 "레드플래그 놓침이 치명" 요구에 정확). 단 정직 한계 = 표준 컨포멀은 marginal 보장뿐 → class-conditional은 레드플래그 표본 충분해야(데이터 병목). API 예제는 docs(classification quickstart)에 있음.

출처: github.com/scikit-learn-contrib/MAPIE · 참조[10 Bates][11 CRC][12 LTT] · `refs/raw/mapie-readme.md`

---

## 3. DeepEval — eval 하네스 + CI 게이트 `[BP검증]`

**무엇:** pytest-native LLM 평가(50+ 메트릭). 평가셋을 테스트로 → 매 커밋 실행 → 임계 미달 시 빌드 실패.

**우리 매핑:** 합성 라벨 평가셋(정상/불확실/레드플래그)을 pytest 케이스로. **레드플래그 recall을 게이트 KPI**로(미달 시 exit non-zero). 3-way 이산 라벨은 **exact-match 결정론 채점**(judge 불안정 회피), 근거문 품질만 G-Eval류 judge. 이용진 "평가셋 69문항 CI 게이트" 실적의 직접 계승.

출처: github.com/confident-ai/deepeval · `refs/raw/deepeval-readme.md`·`deepeval-custom-metric.md`

---

## 4. promptfoo — 레드팀(적대 평가) `[BP검증]`

**무엇:** YAML/CLI 평가 + 레드팀(500+ 적대 벡터). assertion 실패 시 exit non-zero.

**우리 매핑:** **권위 사칭·멀티턴 에스컬·응급 오도** 적대 케이스를 레드팀 스위트로 상시 검증(medRxiv 45% 권위사칭 취약을 겨냥). DeepEval(메인 회귀) + promptfoo(레드팀) 2트랙.

출처: github.com/promptfoo/promptfoo · promptfoo.dev/docs/red-team · `refs/raw/promptfoo-*.md`

---

## 5. Anthropic 구조화 출력 — 3-way enum 강제 `[BP검증]`

**무엇:** Claude 구조화 출력(tool use/structured outputs). 단 strict 스키마는 best-effort → **Pydantic 검증 + retry 필수.**

**우리 매핑:**
```python
class Verdict(BaseModel):
    verdict: Literal["정상","불확실","레드플래그"]
    reasons: list[str]
    kb_refs: list[str]          # 근거 KB 항목 ID(인용 강제)
    red_flag_matched: list[str] # 결정론 룰 매칭 결과
```
enum 3값 강제로 파이프라인 안 깨짐(job-radar id 포맷 고정 교훈과 동형). Pydantic v2 + 실패 시 재호출.

출처: platform.claude.com/docs/en/build-with-claude/structured-outputs · anthropic.com/engineering/building-effective-agents · `refs/raw/anthropic-*.md`

---

## 6. NeMo Guardrails — 룰 프레임워크(선택) `[BP검증]`

**무엇:** Colang DSL로 대화 가드레일. **우리 판단 = 도메인 레드플래그는 자작 YAML 룰 + Claude가 더 단순·투명**(BP E절). NeMo는 인젝션·범위이탈 방어 참고용으로만. 조기 도입은 과함.

출처: github.com/NVIDIA/NeMo-Guardrails · `refs/raw/nemo-guardrails-readme.md`

---

## 7. vlm-uncertainty — VLM 기권 설계(아이디어만) `[BP검증]`

**무엇:** RL로 컨포멀 임계값 학습해 single/set/**abstain** 3-mode. **단 logit·모델 인프라 필요 → API-only 재현 어려움**(정직 표기). 차용할 건 "이미지도 불확실하면 기권(abstain 모드)"이라는 **설계 아이디어**뿐. 실제 이미지측 기권은 BP대로 VLM self-consistency + defer-by-default로.

출처: github.com/sinatayebati/vlm-uncertainty(arXiv 2502.06884) · `refs/raw/vlm-uncertainty-readme.md`

---

## 8. 학술 방법 근거 `[읽음: 초록]`

- **MedAbstain**(arXiv 2601.12471, "Knowing When to Abstain: Medical LLMs Under Clinical Uncertainty") — APS 컨포멀 + 명시적 기권옵션. 기권 recall 13% gap의 원출처. → `abstain` 1급 옵션 + 컨포멀 임계값의 방법 근거.
- **Conformal Abstention**(DeepMind arXiv 2405.01563) — LLM 기권에 컨포멀 보장 선례.
- 원문 = `refs/raw/arxiv-*.md`(초록·메타).

---

## 9. 레퍼런스 서비스 (설계·안전문구) — 부분 `[SPA thin]`

- **Ada Health**(트리아지·"진단 안 함" 정본), **Post Op**(사후케어 플로우) — 정적 크롤이 SPA라 얇게 수집됨(Ada 1.7k·Post Op 3.6k). UX·안전 고지 문구가 필요하면 **동적 재수집**(Scrapling `DynamicFetcher`/`StealthyFetcher` + 브라우저 설치)로 후속. 현재는 코드·설계 자산이 1~8로 충분해 보류.

---

## 통합 조립도 (이 자산들이 한 파이프라인으로)

```
입력(증상+사진+시술·경과일)
  → [결정론 레드플래그 룰: 자작 YAML]           (5·6 참고, medRxiv 근거)
  → [KB 라우팅: 정적 YAML full-context, 인용강제]
  → [Claude 판정: 구조화 출력 3-way]             (5: Pydantic enum+retry)
  → [불확실성 게이트: uqlm self-consistency]      (1: BlackBoxUQ)
      + [이미지 defer-by-default: VLM self-consistency]  (7 아이디어)
  → [임계값·recall 보장: MAPIE 컨포멀/CRC]        (2)
  → 정상(안심) / 불확실(기권→의사) / 레드플래그(에스컬)
측정·CI: DeepEval(레드플래그 recall 게이트) + promptfoo(레드팀)  (3·4)
증거물: RC 곡선(게이트 A/B) + 레드플래그 FNR
```

## 크롤 재현
```bash
.venv/Scripts/python.exe refs/crawl.py   # refs/raw/ 갱신 + _manifest.json
```
